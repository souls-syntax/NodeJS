function fam() {
  console.log("Hello world")
}

let obj = {
  study: "web"
}

let place = "iit_chouman"

module.exports = {obj, place, fam}
// When key and the value is same we don't need to write it in key value pair just writing value is sufficient
